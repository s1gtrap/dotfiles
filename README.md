# macOS setup

1. Install [Homebrew](https://brew.sh)

    1. Don't forget to change `.zprofile` for `PATH` changes (as noted after install):
```
% cat .zprofile 

eval "$(/usr/local/bin/brew shellenv)"
```
2. Install iTerm2 with `brew install --cask iterm2`
